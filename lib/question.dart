class Question {
  String questionText;
  bool questionAnswer;

  // Dart Constructor
  // Question({String q, bool a}) {
  //   questionText = q;
  //   questionAnswer = a;
  // }
Question(this.questionText, this.questionAnswer);
}
